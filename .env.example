# APP_URL: The URL where this frontend app is hosted.
# Used for self-referential links and API endpoints.
APP_URL="http://localhost:3000"

# Note: No API keys (Gemini, Supabase, Cloudinary) should be placed in the frontend env.
# All secret keys belong strictly in backend/.env which is ignored by git.
